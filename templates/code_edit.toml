name = "code_edit"
description = "Modify or extend existing files in the workspace."

[slots.TASK_INTERPRETATION]
required = true

[slots.RELEVANT_FILES]
required = true
type = "BulletList<FilePath>"

[slots.PROPOSED_APPROACH]
required = true

[slots.FILE_EDITS]
required = true

[slots.FINAL_RESPONSE]
required = true
